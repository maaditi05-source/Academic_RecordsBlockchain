version: '3.7'

# Node 07 — Departments ECE Peer
# Roles: ECE Faculty + HOD approvals
# Machine IP: ${DEPT_ECE_HOST}

networks:
  fabric_net:
    driver: bridge

volumes:
  peer0.ece.departments.nitw.edu:
  couchdb.ece:


services:

  couchdb.ece:
    container_name: couchdb.ece
    image: couchdb:3.3
    environment:
      - COUCHDB_USER=admin
      - COUCHDB_PASSWORD=adminpw
    ports:
      - "${DEPT_ECE_COUCHDB_PORT:-5987}:5984"
    networks:
      - fabric_net

  peer0.ece.departments.nitw.edu:
    container_name: peer0.ece.departments.nitw.edu
    image: hyperledger/fabric-peer:2.5
    labels:
      service: hyperledger-fabric
    environment:
      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock
      - CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE=fabric_net
      - FABRIC_LOGGING_SPEC=INFO
      - CORE_PEER_TLS_ENABLED=true
      - CORE_PEER_TLS_CERT_FILE=/etc/hyperledger/fabric/tls/server.crt
      - CORE_PEER_TLS_KEY_FILE=/etc/hyperledger/fabric/tls/server.key
      - CORE_PEER_TLS_ROOTCERT_FILE=/etc/hyperledger/fabric/tls/ca.crt
      - CORE_PEER_ID=peer0.ece.departments.nitw.edu
      - CORE_PEER_ADDRESS=peer0.ece.departments.nitw.edu:${DEPT_ECE_PORT:-9151}
      - CORE_PEER_LISTENADDRESS=0.0.0.0:${DEPT_ECE_PORT:-9151}
      - CORE_PEER_CHAINCODEADDRESS=peer0.ece.departments.nitw.edu:${DEPT_ECE_CHAINCODE_PORT:-9152}
      - CORE_PEER_CHAINCODELISTENADDRESS=0.0.0.0:${DEPT_ECE_CHAINCODE_PORT:-9152}
      # Gossip bootstrap from CSE anchor peer
      - CORE_PEER_GOSSIP_BOOTSTRAP=peer0.cse.departments.nitw.edu:${DEPT_CSE_PORT:-9051}
      - CORE_PEER_GOSSIP_EXTERNALENDPOINT=${DEPT_ECE_HOST:-localhost}:${DEPT_ECE_PORT:-9151}
      - CORE_PEER_LOCALMSPID=DepartmentsMSP
      - CORE_PEER_MSPCONFIGPATH=/etc/hyperledger/fabric/msp
      - CORE_OPERATIONS_LISTENADDRESS=peer0.ece.departments.nitw.edu:17053
      - CORE_LEDGER_STATE_STATEDATABASE=CouchDB
      - CORE_LEDGER_STATE_COUCHDBCONFIG_COUCHDBADDRESS=couchdb.ece:5984
      - CORE_LEDGER_STATE_COUCHDBCONFIG_USERNAME=admin
      - CORE_LEDGER_STATE_COUCHDBCONFIG_PASSWORD=adminpw
    volumes:
      - /var/run/docker.sock:/host/var/run/docker.sock
      - ../organizations/peerOrganizations/departments.nitw.edu/peers/peer0.ece.departments.nitw.edu/msp:/etc/hyperledger/fabric/msp
      - ../organizations/peerOrganizations/departments.nitw.edu/peers/peer0.ece.departments.nitw.edu/tls:/etc/hyperledger/fabric/tls
      - peer0.ece.departments.nitw.edu:/var/hyperledger/production
    working_dir: /root
    command: peer node start
    ports:
      - "${DEPT_ECE_PORT:-9151}:${DEPT_ECE_PORT:-9151}"
    depends_on:
      - couchdb.ece
    networks:
      - fabric_net
    extra_hosts:
      - "orderer1.nitw.edu:${ORDERER1_HOST:-host-gateway}"
      - "orderer2.nitw.edu:${ORDERER2_HOST:-host-gateway}"
      - "orderer3.nitw.edu:${ORDERER3_HOST:-host-gateway}"
      - "peer0.nitwarangal.nitw.edu:${NITW_PEER0_HOST:-host-gateway}"
      - "peer0.cse.departments.nitw.edu:${DEPT_CSE_HOST:-host-gateway}"
      - "peer0.me.departments.nitw.edu:${DEPT_ME_HOST:-host-gateway}"
      - "peer0.dac.departments.nitw.edu:${DEPT_DAC_HOST:-host-gateway}"
      - "peer0.verifiers.nitw.edu:${VERI_PEER0_HOST:-host-gateway}"
