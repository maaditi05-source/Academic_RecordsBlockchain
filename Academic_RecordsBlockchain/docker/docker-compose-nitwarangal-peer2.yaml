version: '3.7'

# Node 06 — NITWarangal Peer 2 (Dean Academic)
# Roles: Dean Academic approvals
# Machine IP: ${NITW_PEER2_HOST}

networks:
  fabric_net:
    driver: bridge

volumes:
  peer2.nitwarangal.nitw.edu:
  couchdb.nitw.peer2:


services:

  couchdb.nitw.peer2:
    container_name: couchdb.nitw.peer2
    image: couchdb:3.3
    environment:
      - COUCHDB_USER=admin
      - COUCHDB_PASSWORD=adminpw
    ports:
      - "${NITW_COUCHDB2_PORT:-5986}:5984"
    networks:
      - fabric_net

  peer2.nitwarangal.nitw.edu:
    container_name: peer2.nitwarangal.nitw.edu
    image: hyperledger/fabric-peer:2.5
    labels:
      service: hyperledger-fabric
    environment:
      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock
      - CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE=fabric_net
      - FABRIC_LOGGING_SPEC=INFO
      - CORE_PEER_TLS_ENABLED=true
      - CORE_PEER_PROFILE_ENABLED=false
      - CORE_PEER_TLS_CERT_FILE=/etc/hyperledger/fabric/tls/server.crt
      - CORE_PEER_TLS_KEY_FILE=/etc/hyperledger/fabric/tls/server.key
      - CORE_PEER_TLS_ROOTCERT_FILE=/etc/hyperledger/fabric/tls/ca.crt
      - CORE_PEER_ID=peer2.nitwarangal.nitw.edu
      - CORE_PEER_ADDRESS=peer2.nitwarangal.nitw.edu:${NITW_PEER2_PORT:-7251}
      - CORE_PEER_LISTENADDRESS=0.0.0.0:${NITW_PEER2_PORT:-7251}
      - CORE_PEER_CHAINCODEADDRESS=peer2.nitwarangal.nitw.edu:${NITW_PEER2_CHAINCODE_PORT:-7252}
      - CORE_PEER_CHAINCODELISTENADDRESS=0.0.0.0:${NITW_PEER2_CHAINCODE_PORT:-7252}
      # Gossip bootstrap from anchor peer (peer0)
      - CORE_PEER_GOSSIP_BOOTSTRAP=peer0.nitwarangal.nitw.edu:${NITW_PEER0_PORT:-7051}
      - CORE_PEER_GOSSIP_EXTERNALENDPOINT=${NITW_PEER2_HOST:-localhost}:${NITW_PEER2_PORT:-7251}
      - CORE_PEER_LOCALMSPID=NITWarangalMSP
      - CORE_PEER_MSPCONFIGPATH=/etc/hyperledger/fabric/msp
      - CORE_OPERATIONS_LISTENADDRESS=peer2.nitwarangal.nitw.edu:17251
      - CORE_LEDGER_STATE_STATEDATABASE=CouchDB
      - CORE_LEDGER_STATE_COUCHDBCONFIG_COUCHDBADDRESS=couchdb.nitw.peer2:5984
      - CORE_LEDGER_STATE_COUCHDBCONFIG_USERNAME=admin
      - CORE_LEDGER_STATE_COUCHDBCONFIG_PASSWORD=adminpw
    volumes:
      - /var/run/docker.sock:/host/var/run/docker.sock
      - ../organizations/peerOrganizations/nitwarangal.nitw.edu/peers/peer2.nitwarangal.nitw.edu/msp:/etc/hyperledger/fabric/msp
      - ../organizations/peerOrganizations/nitwarangal.nitw.edu/peers/peer2.nitwarangal.nitw.edu/tls:/etc/hyperledger/fabric/tls
      - peer2.nitwarangal.nitw.edu:/var/hyperledger/production
    working_dir: /root
    command: peer node start
    ports:
      - "${NITW_PEER2_PORT:-7251}:${NITW_PEER2_PORT:-7251}"
    depends_on:
      - couchdb.nitw.peer2
    networks:
      - fabric_net
    extra_hosts:
      - "orderer1.nitw.edu:${ORDERER1_HOST:-host-gateway}"
      - "orderer2.nitw.edu:${ORDERER2_HOST:-host-gateway}"
      - "orderer3.nitw.edu:${ORDERER3_HOST:-host-gateway}"
      - "peer0.nitwarangal.nitw.edu:${NITW_PEER0_HOST:-host-gateway}"
      - "peer1.nitwarangal.nitw.edu:${NITW_PEER1_HOST:-host-gateway}"
      - "peer0.cse.departments.nitw.edu:${DEPT_CSE_HOD_HOST:-host-gateway}"
      - "peer1.cse.departments.nitw.edu:${DEPT_CSE_FAC_HOST:-host-gateway}"
      - "peer0.ece.departments.nitw.edu:${DEPT_ECE_HOD_HOST:-host-gateway}"
      - "peer1.ece.departments.nitw.edu:${DEPT_ECE_FAC_HOST:-host-gateway}"
      - "peer0.verifiers.nitw.edu:${VERI_PEER0_HOST:-host-gateway}"
      - "peer1.verifiers.nitw.edu:${VERI_PEER1_HOST:-host-gateway}"
